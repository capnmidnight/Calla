{
  "version": "1.0.0",
  "name": "calla-site",
  "private": true,
    "scripts": {
        "build": "rollup -c --environment BUILD:production",
        "build:basic": "rollup -c --environment BUILD:basic",
        "build:game": "rollup -c --environment BUILD:game",
        "watch": "rollup -c -w --environment BUILD:development",
        "watch:basic": "rollup -c -w --environment BUILD:basic",
        "watch:game": "rollup -c -w --environment BUILD:game"
    },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^9.0.0",
    "@rollup/plugin-strip": "^2.0.0",
    "rollup-plugin-terser": "^7.0.0",
    "tracekit": "^0.4.5"
  },
  "-vs-binding": {
    "ProjectOpened": [
      "watch"
    ]
  }
}
