@{
    Layout = "_AdminPage";
    ViewData["Title"] = "UserAgents";
}

@model Calla.Data.StringCount[]


@if (Model.Length == 0)
{
    <p>No Content</p>
}
else
{
    <ul>
        @foreach(var row in Model)
        {
            <li>[@row.Count] @row.Value</li>
        }
    </ul>
}