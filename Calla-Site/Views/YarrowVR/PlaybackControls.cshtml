@{
    Layout = "_YarrowVRAdminPage";
    ViewData["Title"] = "Playback Controls";
}

@model System.Collections.Generic.IEnumerable<Yarrow.Data.PlaybackControls>


@if (Model == null || Model.Empty())
{
    <p>No Content</p>
}
else
{
    <table id="output" cellspacing="0">
        <thead>
            <tr>
                <th>Transform</th>
                <th>Audio Track</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var row in Model)
            {
                <tr>
                    <td><a href="~/YarrowVR/Transform/@row.TransformId">@row.TransformId</a></td>
                    <td><a href="~/YarrowVR/AudioTrack/@row.AudioTrackId">@row.AudioTrackId</a></td>
                </tr>
            }
        </tbody>
    </table>
}