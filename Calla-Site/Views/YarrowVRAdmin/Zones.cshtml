@{
    Layout = "_YarrowVRAdminPage";
    ViewData["Title"] = "Zones";
}

@model System.Collections.Generic.IEnumerable<Yarrow.Data.Zones>


@if (Model == null || Model.Empty())
{
    <p>No Content</p>
}
else
{
    <table id="output" cellspacing="0">
        <thead>
            <tr>
                <th></th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var row in Model)
            {
                <tr>
                    <td><a href="~/YarrowVRAdmin/Zone/@row.Id">@row.Id</a></td>
                    <td>@row.Name</td>
                </tr>
            }
        </tbody>
    </table>
}