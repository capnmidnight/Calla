/**
 * Rendering mode ENUM.
 */
export enum RenderingMode {
    /** Use ambisonic rendering. */
    Ambisonic = 'ambisonic',
    /** Bypass. No ambisonic rendering. */
    Bypass = 'bypass',
    /** Disable audio output. */
    None = 'off',
}