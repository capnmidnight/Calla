import type { CanvasTypes } from "../html/canvas";
export declare function sliceImage(img: CanvasImageSource, x: number, y: number, w1: number, h1: number, w2: number, h2: number, rotation: number): CanvasTypes;
