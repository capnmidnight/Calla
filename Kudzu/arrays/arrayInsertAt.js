/**
 * Inserts an item at the given index into an array.
 * @param arr
 * @param item
 * @param idx
 */
export function arrayInsertAt(arr, item, idx) {
    arr.splice(idx, 0, item);
}
//# sourceMappingURL=arrayInsertAt.js.map